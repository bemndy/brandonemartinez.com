name: prod-ci

# Every pull request targeting main, plus main itself after a merge so the
# merged result is actually built. ('branches' filters the PR's *target*
# branch, not the source, so this covers PRs from any feature/bugfix branch.)
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 26
        uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      # TODO: integration tests (playwright) run here only, not in dev-ci.
      - run: npm test --if-present
